# -*- mode: sh; -*-

ncpu=$(sysctl -n hw.ncpu)

nix-grep() {
  nix-env -qaP \* \
    | grep -i "$@";
}
alias ng='nix-grep'

nix-install() {
  nix-env -Q -j$(ncpu) -k -i "$@";
}

nix-update() {
  nix-env -i "$@";
}

nixpkgs-sync() {
  git -C "${NIXPKGS_PATH}" pull --ff-only upstream master;
}
