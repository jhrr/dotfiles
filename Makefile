## -*- mode: makefile-gmake -*-

dot = $(CURDIR)
script = $(CURDIR)/bin

# rm ~/.vimrc
# mkdir ~/.vim-tmp
# ln -s `pwd`/.vimrc ~/.vimrc

# hg clone https://bitbucket.org/gward/vcprompt
# cd vcprompt
# autoconf
# ./configure
# make
# make install
# make install PREFIX=$CURDIR/bin or PREFIX=$CURDIR
# ln -s vcprompt ~/bin/vcprompt
